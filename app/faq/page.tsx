import Link from 'next/link';
import { getSessionUser } from '@/lib/auth';
import PublicNavbar from '@/components/navigation/PublicNavbar';
const qs=[['Can I build from my phone?','Yes. The builder is designed around touch-first editing and responsive previews.'],['Can I change music later?','Yes. Template music layers are edited independently when supported by the template.'],['Can I preview before publishing?','Yes. Preview mode removes editor controls and shows the experience cleanly.'],['Can I undo changes?','Yes. Undo and Redo stay available while you edit.']];
export default async function Page(){const u=await getSessionUser().catch(()=>null);return <main className="premium-site"><PublicNavbar user={u}/><section className="premium-container simple-page"><div className="simple-heading"><p className="section-kicker">FAQ</p><h1>Questions, answered.</h1><p>Everything a new creator needs to know before making their first experience.</p></div><div className="faq-list">{qs.map(([q,a])=><article key={q}><div><span>?</span><h2>{q}</h2></div><p>{a}</p></article>)}</div></section></main>}
